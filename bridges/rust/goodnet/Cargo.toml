[package]
name        = "goodnet"
version     = "0.1.0"
edition     = "2021"
license     = "MIT"
description = "Safe Rust wrapper around the GoodNet kernel C ABI."

[lib]
crate-type = ["rlib"]

[dependencies]
goodnet-sys = { path = "../goodnet-sys", version = "0.1.0" }
thiserror   = "1"
