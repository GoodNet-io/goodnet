[pytest]
testpaths = tests/livedoc tests/tools
pythonpath = tools
addopts = -ra --strict-markers --tb=short
markers =
    integration: tests that touch the real working tree rather than the conftest fixture
