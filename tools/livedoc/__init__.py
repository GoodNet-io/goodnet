"""livedoc — source-derived injections into hand-written markdown.

Tooling that extracts facts from sdk/*.h via libclang, computes
roadmap status from plugin trees, and rewrites placeholder blocks
inside the existing narrative markdown in docs/. Run periodically
through `tools/livedoc.py --all`.
"""
